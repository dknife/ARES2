# ARES2 개발 노트

> 세션별 주요 작업·결정사항 기록. 최신 항목을 위에 추가한다.
> (코드 세부는 git 로그 참조 — 여기엔 "왜/무엇을 정했는지"와 현재 상태를 남긴다.)

---

## 2026-07-03 — 전체 코드 리뷰 · HIGH 4건 + MEDIUM 5건 수정 (남은 작업 목록)

전체 코드 리뷰(웹·펌웨어·시뮬레이터·프로토콜 정합성)에서 발견한 결함 중
HIGH 4건(자석 표시 반전·설정 오타 부팅 불가·BLE 응답 desync·비상정지 지연)과
MEDIUM 5건(한글 명령 유실·블록 인터프리터·UI 배선·대시보드 폴링·dt 애니메이션)을
수정 완료. 상세는 진행보고 9일차(`Document/진행보고/`)와 git 로그
(`8ddea72`, `107d26f`) 참조.

### 남은 수정 작업 (리뷰에서 발견, 미처리)

**즉시 할 일**
- [ ] **Pico 펌웨어 재업로드** — `main.py`·`process_data.py`·`system_config.py`가
  수정됐으나 실기 반영은 재업로드해야 함 (Thonny/ampy).

**MEDIUM (보류 2건)**
- [ ] **`SET_PIN` 미적용** — 설정 파일에 저장·성공 응답까지 반환하지만 OLED 제외
  7개 모듈 생성자는 `pins.py` 하드코딩 상수만 읽음 → 재부팅해도 핀 안 바뀜.
  계획: `hardware.py`에 `_get_pin(config_key, default)` 헬퍼를 만들어 OLED가 이미
  쓰는 패턴(설정에서 핀 읽기, `pins.py`가 기본값)을 나머지 모듈에 확장.
- [ ] **`Web/Simulation/` 죽은 코드** — 현재 main은 `simulation.js` →
  `Sim_Parts/context.js` 직접 배선이고 `Simulation/*.js`는 어디서도 import 안 됨.
  **main에서 손대지 말 것** — `Simulation_LeeMinhyuck` 브랜치가 `Simulation_Main`
  경유 재배선을 진행 중(Simulation_Base 신설). **머지 리뷰 때 확인**: 래퍼의
  `movement.dispose()`/`oled.dispose()` 호출 대상이 실존하는지(구 코드엔 없어서
  TypeError). CLAUDE.md의 "Simulation_Main.js로 배선" 설명도 머지 전까진 사실과 다름.

**LOW (여유 있을 때)**
- [ ] `CALIB_VALUES,` 응답 파서(`bluetooth.js`·`dashboard.html`) — 펌웨어가 안 보내는
  죽은 경로. 제거하거나 펌웨어에 응답 추가.
- [ ] `SING` 주석 모순 — 웹(`commandexecutor.js`)·펌웨어(`main.py`) 둘 다 주석은
  "fire-and-forget에 넣지 말 것"인데 코드는 넣어놓음. 동작은 양쪽 일치하므로
  주석만 고치되 **한쪽만 고치면 desync** 주의.
- [ ] `DIST:ERROR`/`MAG:ERROR` — 웹 정규식이 숫자만 매칭해 모듈 없음 오류가
  아이 화면에서 이전 값으로 보임. 오류 표시 경로 추가.
- [ ] 시뮬레이터: traffic 토픽에서 `BUZZER_ON` 무반응(`dispatch.js` — cleanups가
  비어 audio 재생 전 return), UI가 "LED 3"이라 부르는 가슴 LED가 `LED_ON,3`으로
  안 켜짐, `MISSION_TOPIC`이 빈 객체라 미션별 토픽 선택이 항상 albi.
- [ ] 펌웨어: 부저 tick 연산 wraparound 미대응(`buzzer.py` — `ticks_add/diff`로 교체),
  `CALIB_START`가 OLED 비활성 시 휠 캘리브레이션 불가(OLED를 선택적으로),
  소수점 설정값(`max_speed=40.5`)이 문자열로 남아 모터 명령 조용히 실패
  (`system_config.py:load_config` 파싱), 장치명에 쉼표 시 `SYS_VALUES` 필드 밀림.
- [ ] 문서: `API_DOCUMENTATION.md` — 시간지정 명령의 속도 인자 누락,
  `SYS_VALUES` 7번째 필드(active_model) 누락. CLAUDE.md — `MAIN_FORWARD`는
  웹이 안 보내는 죽은 명령, "런타임 핀 변경은 재부팅 필요" 설명은 SET_PIN 구현 전까지 부정확.
- [ ] `assign_variable` 죽은 코드(`commandexecutor.js`·`blocklyconfig.js`
  BATCH_FORBIDDEN_TYPES) — 존재하지 않는 블록 타입. 제거.
- [ ] 대시보드 20000ms 타임아웃이 **localStorage에 이미 저장된 사용자**는 여전히
  20000 로드됨(`ares-system-config`) — 마이그레이션 또는 상한 클램프 고려.

### 새로 생긴 규약 (수정 시 유지할 것)
- 시간지정 동작 상한 **60초**: 웹 `commandexecutor.js MAX_TIMED_SEC` ↔ 펌웨어
  `process_data.py MAX_TIMED_SEC` 동기 유지.
- 값 반환 명령의 기대 응답 접두어: `bluetooth.js _expectedResponsePrefix()` —
  새 GET_* 명령 추가 시 여기에도 등록.
- 펌웨어 수신 버퍼는 **bytes** (`main.py rx_buffer`) — str로 되돌리지 말 것.
- 반환값 함수는 `_resolveFunctionCalls()` 비동기 사전 해석 경유 — 값 평가 경로를
  새로 만들면 사전 해석 호출을 잊지 말 것.

---

## 2026-07-02 — 서비스 UX 개편 (개요 통합 · 하단바 · 애니메이션)

### 개발 문서
- `dev.html`이 이제 **3부 구성**: ① 1차 Refactoring 계획 ② 2차 Refactoring 계획 ③ 디자인 계획.
  상단에 파트 점프 내비 존재. 원문은 `Document/REFACTORING_PLAN*.md`, `DESIGN.md`.

### 서비스 첫 화면 (`Web/index.html`)
- 인사 문구 축약: `안녕! 나는 탐사로봇 알비야 👋` + `이제 탐험을 시작해!` (이전 장문 안내 제거).

### 개요 화면 = 단일 통합 아코디언 (`Web/overview.html`, `main.js`, `styles.css`)
- 기존 개요·차시·미션 3단계를 **개요 안 중첩 아코디언**으로 통합.
  - 차시 클릭 → (위) **차시 정보**(푸른색 팔레트) + (아래) **미션 리스트**.
  - 미션 클릭 → 그 아래로 **미션 상세**(이야기·목표·샘플코드) + `🧩 미션 코딩 시작` 버튼.
  - `미션 코딩 시작` → 해당 미션 선택 후 **블록코딩 모드**로 진입.
- 관련 헬퍼: `renderInlineMissionItem/Detail`, `renderInlineLessonInfo`, `toggleInlineMissionDetail`, `openMissionCoding`.
- 색 구분: **차시 정보 = 파랑 계열**, **미션 = 주황 계열**.

### 하단 모바일 내비게이션 (`Web/main.html`, `main.js`, `styles.css`)
- 최종 구성 4탭: **🚀 미션 · 🧩 코딩 · 🤖 AI · 📝 로그** (그리드 `repeat(4, …)`).
  - 이전 개요·차시·미션 3탭 → `미션` 단일 탭으로 통합했다가, 이후 `코딩` 탭 추가.
- **미션 탭** = 개요 메뉴(아코디언) 표시. **코딩 탭** = 기록된 미션의 블록코딩으로 바로 진입.
- 활성 표시: 미션(코딩 영역) 뷰 → `코딩` 탭, 개요·차시 메뉴 → `미션` 탭.

### 선택 미션 기록 (localStorage 키)
- `ares_last_coding_mission_v1` → `{ lesson, mission }`. `enterMission()`에서 기록(`rememberCodingMission`).
- `코딩` 탭과 `AI` 탭이 이 값을 공유해, **1차시 1미션으로 리셋되지 않고 마지막 미션 유지**.
- (참고) 미션 완료 진행도 키: `ares_completed_missions_v1`.

### 애니메이션 (`styles.css` + `main.js`)
- **아코디언 여닫이**: 열림 `2s` / 닫힘 `1s`. 실제 콘텐츠 높이를 `--acc-h`로 측정해 자연스럽게.
  제어는 `openAccordion()/closeAccordion()` (클래스 `accordion-opening/closing`).
- **화면 전환**: 개요/차시/미션 뷰 전환 = 슬라이드-인, 미션 내부 모드(설명↔코딩↔시뮬) = 페이드. 둘 다 `1s`.
- `prefers-reduced-motion` 사용자는 애니메이션 생략.

### 모바일 캐시 이슈 (미해결 — 다음 작업 후보)
- 증상: PC는 최신인데 **모바일은 이전 화면**. 원인은 서비스워커 아님(없음) → **브라우저 HTTP 캐시**.
- 핵심: `main.html`이 `styles.css`/`main.js`를 **버전 없이** 참조 → 모바일이 옛 JS/CSS 유지.
- **주의(중요)**: `main.js`는 `type="module"`로 8개 모듈을 import. **엔트리 스크립트에 `?v=`를 붙여도
  내부 `import`문에는 전파되지 않음** → 하위 모듈만 고치면 반영 안 됨.
  → 날짜 쿼리를 엔트리에만 붙이는 방식은 부적합. **import map** 또는 **빌드 시 버전 자동 주입** 권장(미적용).
- 임시 우회: 시크릿 탭 / 캐시 삭제 / URL `?v=` 쿼리.

### 다음에 할 만한 것
- 자산 캐시버스팅(import map or build.* 자동 주입) 적용.
- 화면 전환을 View Transitions API로 "진짜 밀어내기(push)"로 업그레이드(iOS 18+ 지원, 구형은 폴백).
